---
- name: Install system packages and enable services
  ansible.builtin.import_tasks: packages.yml
  tags: [server, packages]

- name: Configure firewall rules
  ansible.builtin.import_tasks: firewall.yml
  tags: [server, firewall]

- name: Verify hardware virtualization
  ansible.builtin.import_tasks: virtualization.yml
  tags: [server, virtualization]

- name: Configure network bridge
  ansible.builtin.import_tasks: network.yml
  tags: [server, network]

- name: Configure maintenance schedule
  ansible.builtin.import_tasks: maintenance.yml
  tags: [server, maintenance]
